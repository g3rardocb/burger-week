<!DOCTYPE html>
<html lang="es">
<head>
  <title>Editar Restaurante</title>
</head>
<body>
  <h1>Editar Restaurante</h1>
  <form action="/admin/restaurantes/editar/<%= restaurante.id %>" method="POST" enctype="multipart/form-data">
    <label>Nombre:</label>
    <input type="text" name="nombre" value="<%= restaurante.nombre %>" required>
    
    <p>Logo actual: <img src="<%= restaurante.logo %>" width="100" alt="<%= restaurante.nombre %>"></p>
    <p>Si deseas cambiar el logo, sube uno nuevo:</p>
    <input type="file" name="logo" accept="image/*">
    
    <button type="submit">Guardar Cambios</button>
  </form>
</body>
</html>